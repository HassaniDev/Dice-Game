'use strict';

// STATES
const state = {
  currentPlayer: 0,
  currentScore: 0,
  totalScore: 0,
};

// Bundle everything into a single object
const modal = { state };

// Export the entire `modal` object
export default modal;
